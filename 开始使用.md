# 项目代码与数据

仓库：https://github.com/tomatot3/Structure-Generation-of-Interlocking-Floral-Patterns

先下载代码，再从仓库Releases下载数据包并解压到代码根目录。解压后根目录应同时有 `code/`、`web/`、`data/` 和 `preview.html`。

- `PaperA_Generated_Dataset_1128_Structures_500_Pairs_v1.0.0.zip`：1128份结构资产及500对结构与渲染图，包含语义SVG、几何JSON、PNG、索引和对应提示词。
- `PaperA_Experiment_Reproduction_Data_v1.0.0.zip`：108个对照上下文及候选数据，用于复现选择算法比较。

数据包不含118份来源标注SVG及其内嵌来源图。仓库只保留来源索引。

双击 `preview.html` 可离线浏览配对图集。生成代码的使用见 `README.md`；网页运行见 `web/README.md`。下载的数据目录已由 `.gitignore` 排除，后续提交代码不会重新上传全部图件。
