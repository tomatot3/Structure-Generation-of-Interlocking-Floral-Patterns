# Release preparation

Local package date: 20 September 2026.

Included: current structure assets and paired renderings; recorded seeds and numerical settings; source metadata; individual evaluation observations; corrected comparison results; runnable generation and selection code.

Before public deposit, add the final title, authors, ORCIDs and preferred citation; select licenses for original code and data; and add the repository DOI or permanent URL. This local package has not been uploaded and does not assign a license.

The 118 source-annotation SVGs and embedded reference images are retained locally outside the repository. `source_materials` contains their existing metadata. Citation metadata can be supplied once, in the repository record and the package README, when the author list and title are final.
