# Local website checks — 20 September 2026

The current website was built and exercised through its HTTP API and browser interface.

- The six prototypes each completed a soft-density request, using the existing production generator. Complete job times in this local run ranged from about 10 to 47 seconds.
- An SW1-C prescribed-count request produced five ordinary parents and three children; its two support paths complete the prescribed total L1 count of seven.
- A fixed-floral-configuration comparison preserved the actual main-vine, flower and support geometry while changing the ordinary branches.
- Completed jobs exported SVG, PNG, geometric JSON and generation parameters. Independent output crossing, clearance, flower-region and periodic-seam checks returned no violations in these requests.
- The collection query resolved SW1-C-83 to E10V2_A531. The corresponding structure, rendering and parameter downloads were checked. All 500 browsing thumbnails were generated from the existing images.
- Parent/child selection and its description were exercised with the keyboard. The image-pair dialog supports Escape and focus return. English and Chinese views were inspected.
- The revised studio initially shows only the main vine and flower sites. Generating SW1-A produced a result with selectable ordinary branches and an editor entry.
- In the revised comparison interface, A was selected from recent results and B was generated with a different density and seed. The stored outputs have identical floral geometry and different branch configurations. Selecting a local PNG also displayed it as a visual comparison.
- The reused editor was exercised through browser pointer actions. Moving a tip changed the stored Bézier coordinates; undo and redo restored the corresponding forms. Moving a parent also changed its child's displayed geometry. Saving and reloading worked, and the edited PNG was exported.
- The editor adapter and voluntary submission routes were tested with a temporary store. Saving alone created no feedback record; missing consent was rejected; explicit consent stored matching before/after geometry and parameters. Reopening an edited version preserves its session and constraints. The review exporter reads the submitted geometry changes.
- Browser test edits are private local drafts. They were not submitted to the research collection.

The website is local and binds to 127.0.0.1:8976. Public hosting is not configured. These checks establish the current interactions and output linkage; visual review by the author remains open.
